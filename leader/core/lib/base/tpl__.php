<?php

/**
 * 模板引擎（正在实现中）
 * @author Oreki
 * @since 2014-5-19
 */

class TplUtil {
	
	private static $str;	//整个模板的字符串
	
	/**
	 * 替换的统一过程
	 * @param unknown $str
	 */
	public static function replaceTpl($str){
		//$str = preg_replace('/[\$\{.+\}]/', $replacement, $subject);
	}
	
	private static function replaceVariable(){
		
	}
	
}